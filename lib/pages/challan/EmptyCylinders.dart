import 'package:flutter/material.dart';

class EmptyCylinders extends StatelessWidget {
  const EmptyCylinders({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('123'),
    );
  }
}
